1. reference
- 객체를 가르키는 주소를 담고있는 변수
- ClassName class1 = new ClassName(); 으로 생성한 객체의 경우 class1이 reference
- 생성된 객체의 Memory Address를 class1이 가르킨다.

2. Gabage Collector
- reference가 존재하지 않는 객체를 제거

3. null과 NullPointException
- reference에 null이 저장되면 객체의 연결이 끊기며, 더 이상 객체를 이용할 수 없다.