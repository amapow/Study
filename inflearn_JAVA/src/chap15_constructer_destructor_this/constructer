1. Default Constructer
- 객체가 생성될 때 가장 먼저 호출되는 생성자(명시를 하지 않아도 컴파일 시점에 자동 생성)

2. User Defined Constructer
- Default Constructer 외에 특정 목적에 의해서 개발자가 만든 Constructer, Parameter에 차이가 있다.

3. Destructer
- finalize()
- 객체가 GC에 의해서 메모리에서 제거 될 때 finalize() Method가 호출된다.

4. this
- 객체 자신의 변수를 가르킴
